module Main exposing (Model, Msg(..), init, main, subscriptions, update, view)

import Browser
import Html exposing (..)
import Html.Events exposing (..)
import Tile exposing (..)



-- MAIN


main =
    Browser.element
        { init = init
        , update = update
        , subscriptions = subscriptions
        , view = view
        }



-- MODEL


type alias Model =
    List Tile


init : () -> ( Model, Cmd Msg )
init _ =
    -- ( getTilePermutations [] 0 <| (Tile.createTile () |> addLimb 0 |> addSide 1 Large NoColor |> addLimb 5 |> addSide 6 Large NoColor |> addLimb 10 |> addSide 11 Tiny NoColor)
    -- ( [ Tile.createTile () |> addLimb 0 |> addSide 1 Large NoColor |> addLimb 5 |> addSide 6 Large NoColor |> addLimb 10 |> addSide 11 Tiny NoColor ]
    ( []
    , Cmd.none
    )



-- UPDATE


type Msg
    = NoOp


update : Msg -> Model -> ( Model, Cmd Msg )
update msg model =
    case msg of
        NoOp ->
            ( model, Cmd.none )



-- SUBSCRIPTIONS


subscriptions : Model -> Sub Msg
subscriptions model =
    Sub.none



-- VIEW


view : Model -> Html Msg
view model =
    div []
        [ text (List.length model |> String.fromInt |> (++) "Number of tiles: ")

        -- , drawTile
        --     (Tile.createTile ()
        --         |> addSide 0 Tiny NoColor
        --         |> addLimb 1
        --         |> addLimb 2
        --         |> addSide 3 Small Red
        --         |> addLimb 5
        --         |> addSide 6 Medium Green
        --         |> addLimb 9
        --         |> addSide 10 Tiny (Custom "aa23b7")
        --         |> addLimb 11
        --     )
        --     (Offset 0 100)
        ]


getTilePermutations : List Tile -> Position -> Tile -> List Tile
getTilePermutations list position tile =
    [ tile ]



-- case tile of
--     Invalid _ ->
--         list
--     Complete _ ->
--         tile :: list
--     Incomplete _ ->
--         List.foldr
--             ((++) << getTilePermutations list (position + 1))
--             []
--             [ addLimb position tile
--             , addSide position Tiny NoColor tile
--             , addSide position Small NoColor tile
--             , addSide position Medium NoColor tile
--             , addSide position Large NoColor tile
--             ]
